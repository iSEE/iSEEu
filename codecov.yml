comment:
    layout: "header, diff, changes, suggestions"
    behavior: default

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        informational: true
    patch:
      default:
        target: auto
        threshold: 1%
        informational: true
